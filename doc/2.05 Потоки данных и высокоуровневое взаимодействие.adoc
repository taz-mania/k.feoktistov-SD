=== 2.5. Потоки данных и высокоуровневое взаимодействие

==== 2.5.1. Потоки по процессам

[cols="1,1,2", options="header"]
|===
| Источник | Приемник | Содержание потока (верхнеуровнево) 

3+| **Внешние системы → Интеграционный периметр**
| Кассовая система | Адаптер касс | Отдает потоковые события фиксации продаж
| Складская система | Адаптер склада | Передаё данные о перемещениях и пополнении полок
| Система заказов | Адаптер заказов | Направляет запросы на резервирование/освобождение товаров
| ERP-система | Адаптер ERP | Отправляет измениеи мастер-данных товаров (справочники)
| Платформа Лояльности | Клиент Лояльности | Предоставляет профили клиентов и правила ранжирования
| Службы доставки | Адаптер служб доставки | Запрашивают проверки доступности товаров
| Движок цен | Event Bus [`promo.upcoming`] | Пишет триггеры предстоящих акций (для прогрева)

3+| **Интеграционный периметр → Шина данных**
| Адаптер касс | Event Bus [`inventory.events`] | Пишет нормализованные события продаж
| Адаптер склада | Event Bus [`inventory.events`] | Пишет события складских перемещений
| Адаптер заказов | Сервис резервирования | Отдает команды управления резервами (напрямую синхронно)
| Сервис резервирования | Адаптер заказов | Возвращает результаты резервирования (подтверждения/отказы)
| Адаптер ERP | Event Bus [`config.masterdata`] | Пишет события изменения мастер-данных
| Адаптер ERP | *DB* Метаданные | Записывает справочники SKU в БД
| Адаптер служб доставки | Сервис запросов | Запрашивает наличие (через LB/API GW)

3+| **Обработка команд (Запись)**
| Event Bus [`inventory.events`] | Сервис команд | Читает события от POS/WMS для обработки
| Сервис команд | *DB* Хранилище событий | Записывает факты изменений остатков в БД неизменяемого журнала [Append]
| Сервис команд | Event Bus [`inventory.processed`] | Пишет обогащенные события для последующих компонентов в цепочке обработки
| *DB* Хранилище событий | Захват изменений (CDC) | Читает из транзакционного журнала лог изменений для репликации

3+| **Построение моделей чтения (Прогрузка)**
| Захват изменений | Event Bus [`db.changes`] | Записывает события изменений для репликации
| Event Bus [`inventory.events`] | Загрузчик моделей чтения | Читает события для построения проекций (прогрузки)
| Загрузчик моделей чтения | *DB* Хранилище моделей чтения | Пишет денормализованные агрегаты остатков в БД
| Загрузчик моделей чтения | *DB* Распределенный кэш | Производит предварительную загрузку горячих данных в кеш
| Event Bus [`cache.updates`] | Сервис синхронизации кэша | Читает команды инвалидации/обновления кеша
| Сервис синхронизации кэша | *DB* Распределенный кэш | Записывает обновленные значения остатков в кеш [Update/DEL]

3+| **Прогрев кэша**
| Движок цен | Event Bus [`promo.upcoming`] | Записывает триггеры предстояших акций
| Event Bus [`promo.upcoming`] | Сервис прогрева кэша | Получает задания на прогрев акционных SKU
| *БД* прогрева кэша | Сервис прогрева кэша | Читает реестр заданий прогрева
| Сервис прогрева кэша | *БД* прогрева кэша | Пишет статус заданий прогрева
| *DB* Хранилище моделей чтения | Сервис прогрева кэша | Читает актуальные остатки для загрузки
| Сервис прогрева кэша | *DB* Распределенный кэш | Записывает прогретые ключи акционных SKU в кеш

3+| **Чтение клиентами**
| *DB* Распределенный кэш | Сервис запросов | Читает текущие остатки
| *DB* Хранилище моделей чтения | Сервис запросов | Выполняет резервное чтение при отсутствии в кэше
| Сервис запросов | Гео-сервис | Запррашивает поиск ближайших магазинов
| *DB* Гео-индекс  | Гео-сервис | Читает координаты и зоны покрытия
| *DB* Распределенный кэш | Гео-сервис | Фильтрует магазины по наличию SKU [Read]
| Сервис запросов | Клиент Лояльности | Отправляет запросы данных персонализации
| *DB* Кэш лояльности | Клиент Лояльности | Читает кэшированные профили
| Клиент Лояльности | Платформа Лояльности | Запрашивает актуальные профили при отсутствии в кеше 

3+| **Резервирование (Транзакция)**
| Адаптер заказов | Сервис резервирования | Направляет запросы на бронирование
| Сервис резервирования | Менеджер блокировок | Запросы на установку/снятие распределенной блокировки
| Менеджер блокировок | *DB* Распределенный кэш | Записывает/удаляет lock-ключей в кеш
| *DB* Распределенный кэш | Менеджер блокировок | Читает lock-ключи в кеше
| Сервис резервирования | *БД* резервирования | Записывает факты резервирования в БД
| *DB* Распределенный кэш | Сервис резервирования | Читает доступный остаток
| Сервис резервирования | *DB* Распределенный кэш | Уменьшает доступный остаток
| Сервис резервирования | Event Bus [`reservation.lifecycle`] | Публикует событие жизненного цикла резервирования
| Event Bus [`reservation.lifecycle`] | Сервис синхронизации кэша | Читает уведомления о резервировании для декремента кэша

|===


[cols="2,2,3,2,2", options="header"]
|===
| Источник | Приемник | Содержание потока (верхнеуровнево) | Топик Event Bus (при использовании) | Тип доступа к БД (Read/Write)

5+| **Резервирование (Transaction Path)**
| Адаптер заказов | Сервис резервирования | Запросы на бронирование | — | —
| Сервис резервирования | Менеджер блокировок | Запросы分布式锁 (lock/unlock) | — | —
| Менеджер блокировок | Распределенный кэш | Хранение lock-ключей (SET NX/DEL) | — | Read/Write
| Сервис резервирования | БД резервирования | Запись фактов резервирования (ACID) | — | Write
| Сервис резервирования | Распределенный кэш | Атомарное уменьшение доступного остатка (CAS) | — | Read/Write
| Сервис резервирования | Event Bus | События жизненного цикла резервов | `reservation.lifecycle` | —
| Event Bus | Сервис синхронизации кэша | Уведомления о резервировании для декремента кэша | (подписка) | —

5+| **Сверка и конфликты**
| Event Bus | Сервис сверки | Поток событий для анализа расхождений | `inventory.events` | —
| Сервис сверки | БД конфликтов | Запись выявленных расхождений и правил | — | Write
| Сервис сверки | Event Bus | Авторитетные корректирующие события | `inventory.adjustments` | —
| Event Bus | Сервис команд | Прием корректирующих событий (повторный процесс) | — | —

5+| **Администрирование**
| Админ-консоль | Админ-сервис | Команды управления настройками | — | —
| Админ-сервис | Метаданные | Чтение и запись справочников, конфигураций | — | Read/Write
| Админ-сервис | Event Bus | События изменения конфигурации (safety stock) | `config.changes` | —
| ТСД | API GW | События коррекции остатков, инвентаризации | — | —
| API GW | Сервис команд | Передача команд коррекции от ТСД | — | —
| BI-инструмент | Хранилище временных рядов | Аналитические запросы (оборачиваемость) | — | Read
| BI-инструмент | Хранилище моделей чтения (через адаптер) | Агрегированная статистика | — | Read

5+| **Архивация и метрики**
| Event Bus | Хранилище временных рядов | Потоковая запись метрик и событий (агрегация) | — | Write
| Хранилище временных рядов | Архивное хранилище | Экспорт исторических срезов (batch) | — | Write (порт H)
| БД резервирования | Архивное хранилище | Архивация истории резервов (через порт H) | — | Write
| Event Bus | Очередь ошибок | Сообщения с исчерпанными попытками обработки | `*.dlq` | —
| Очередь ошибок | Админ-сервис | Чтение для ручного разбора ошибок | — | Read

5+| **Внутренние вспомогательные (не показаны явно как потоки, но важны)**
| Все сервисы | Управление секретами | Запросы ключей и сертификатов | — | Read
| Управление секретами | Хранилище секретов | Запросы кэшированных и актуальных секретов | — | Read
| Все сервисы | Реестр схем | Запросы схем для сериализации | — | Read (из БД реестра)
|===

**Примечание:** В таблице использованы согласованные ранее русскоязычные названия компонентов. Топики Event Bus указаны условно для иллюстрации логических потоков; в реализации могут использоваться единый топик `inventory.events` с типизацией сообщений или разделение на специализированные топики.
